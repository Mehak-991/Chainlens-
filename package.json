{
  "name": "chainlens",
  "version": "0.1.0",
  "private": true,
  "description": "ChainLens Supply Chain Risk Explorer — root orchestration package.",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --prefix-colors \"cyan,green\" --names \"BACKEND,FRONTEND\" \"powershell -Command \\\"Set-Location backend; .venv/Scripts/python.exe -m uvicorn app.main:app --reload --port 8000\\\"\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
